{
  "version": 2,
  "buildCommand": "npx prisma generate && npx prisma migrate deploy",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.ts"
    }
  ]
}